#include <bits/stdc++.h>

using namespace std;

int main() {
  int Youngest;
  int Middle;
  int Oldest;
  cin >> Youngest;
  cin >> Middle;
  if (Oldest = (Middle - Youngest + Middle)) {
    cout << Oldest << endl;
  }
  return 0;
}