#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    // out to a current folder
    ofstream fout("code.out");
    // Always write text to file
    string str = "3DS";
    fout << str;

    int a = 1; 
    fout << "a is" << a << endl;
     
    ofstream fout ("path_to_code/code.out")

    ifstream fin("code.in");
    // Be care of variable of data type
    string str;
    fin >> str;
    cout << str;

    ifstream cin("path_to_code/cide.in"); // replace console
    input
    int x, y, z;
    cin >> x; 
    cin >> y; 
    cin >> z;

    return 0;
}